# =============================================================================
# TaskHub API — Project Structure
# Last updated: Post-testing (all 8 milestones complete + CI fixes)
#
# This structure reflects the ACTUAL file paths in the repository,
# verified from the path comment at the top of each file.
#
# Milestone history:
#   M1 ✅ Docker production hardening
#   M2 ✅ Terraform production structure
#   M3 ✅ CI/CD hardening
#   M4 ✅ Kubernetes + Helm
#   M5 ✅ Monitoring stack
#   M6 ✅ Incident response docs
#   M7 ✅ Cost & scaling awareness
#   M8 ✅ Interview polish + README overhaul
# =============================================================================

taskhub-api/
│
├── app/
│   ├── __init__.py
│   ├── main.py                            # FastAPI entrypoint, middleware, /metrics
│   ├── config.py                          # Settings via pydantic-settings + .env
│   ├── db.py                              # Async SQLAlchemy engine + session factory
│   ├── events.py                          # EventBridge publisher helpers (prod-only)
│   ├── idempotency.py                     # SQL-backed idempotent job helpers
│   │
│   ├── routes/
│   │   ├── auth.py                        # Register + login endpoints
│   │   └── tasks.py                       # Task CRUD + paginated list
│   │
│   ├── schemas/                           # Pydantic v2 API contracts
│   │   ├── user_schema.py
│   │   ├── task_schema.py
│   │   ├── pagination_schema.py
│   │   └── token_schema.py
│   │
│   ├── models/                            # SQLAlchemy ORM models
│   │   ├── base.py
│   │   ├── user.py
│   │   ├── task.py
│   │   └── job_log.py
│   │
│   ├── workers/
│   │   ├── __init__.py
│   │   ├── celery_app.py                  # Celery app instance + broker config
│   │   └── tasks/
│   │       ├── __init__.py
│   │       └── email_tasks.py             # Background email task definitions
│   │
│   ├── utils/
│   │   └── security.py                    # Password hashing + JWT helpers
│   │
│   └── tests/
│       ├── conftest.py                    # Fixtures: schema, clean_db, Celery mock
│       ├── test_api.py                    # Health check + auth endpoint tests
│       ├── test_tasks.py                  # Task CRUD + pagination tests
│       └── test_idempotency.py            # Idempotent job execution tests
│
├── alembic/
│   ├── env.py                             # Async migration runner
│   ├── script.py.mako                     # Migration file template
│   └── versions/
│       └── 0001_init_schema.py            # Initial schema migration
│
├── infra/
│   ├── ecs-tasks/
│   │   ├── api-task-definition.json       # ECS API task definition (reference)
│   │   └── worker-task-definition.json    # ECS worker task definition (reference)
│   └── terraform/
│       ├── main.tf                        # VPC, subnets, SGs, RDS, Redis, ALB
│       ├── ecs.tf                         # ECS cluster, task defs, Secrets Manager
│       ├── iam.tf                         # IAM roles (task_exec + task_role)
│       ├── ecr.tf                         # ECR repos + lifecycle policies
│       ├── lambda.tf                      # EventBridge rule + Lambda
│       ├── migration-task.tf              # Alembic migration ECS task
│       ├── alarms.tf                      # CloudWatch alarms + SNS topic
│       ├── autoscaling.tf                 # ECS App Autoscaling (CPU + memory)
│       ├── secrets.tf                     # Secrets Manager (db_password, jwt_secret)
│       ├── monitoring.tf                  # CloudWatch dashboard
│       ├── eks.tf                         # EKS cluster (interview reference)
│       ├── provider.tf                    # AWS provider + S3 remote state backend
│       ├── variables.tf                   # All input variables with defaults
│       ├── outputs.tf                     # Resource output values
│       ├── terraform.tfvars.example       # Safe-to-commit example values
│       └── .terraform.lock.hcl           # Provider version lock file
│
├── k8s/                                   # Raw Kubernetes manifests
│   ├── configmap.yaml                     # Non-sensitive env vars
│   ├── deployment-api.yaml                # API deployment (probes, resources)
│   ├── deployment-worker.yaml             # Celery worker deployment
│   ├── service.yaml                       # ClusterIP service + ALB Ingress
│   └── hpa.yaml                           # Horizontal Pod Autoscaler
│
├── helm/
│   └── taskhub/
│       ├── Chart.yaml                     # Chart metadata
│       ├── values.yaml                    # Default values (overridable per env)
│       └── templates/
│           ├── _helpers.tpl               # Reusable label helpers
│           ├── configmap.yaml
│           ├── deployment-api.yaml
│           ├── deployment-worker.yaml
│           ├── service.yaml
│           └── hpa.yaml
│
├── monitoring/
│   ├── docker-compose.monitoring.yml      # Prometheus + Grafana services
│   ├── prometheus.yml                     # Scrape config (API + self)
│   ├── grafana-dashboard.json             # 7-panel production dashboard
│   └── grafana/
│       └── provisioning/
│           ├── datasources/
│           │   └── prometheus.yml         # Auto-configure Prometheus datasource
│           └── dashboards/
│               └── dashboards.yml         # Auto-load dashboard on startup
│
├── docs/
│   ├── ARCHITECTURE.md                    # System design + data flow
│   ├── SECURITY.md                        # Auth, secrets, OWASP headers
│   ├── FAILURE_MODES.md                   # Per-layer failure + recovery
│   ├── ECR.md                             # ECR push guide
│   ├── INCIDENTS.md                       # 3 runbooks with CLI commands
│   ├── OPERATIONS.md                      # AWS/K8s/Linux/Prometheus cheat sheet
│   ├── COST.md                            # Monthly breakdown + reduction strategies
│   └── AWS_DEPLOY.md                      # Final AWS deployment checklist
│
├── scripts/
│   ├── push-to-ecr.sh                     # Build + tag + push to ECR
│   └── run-migrations.sh                  # Run Alembic migrations via ECS task
│
├── .github/
│   └── workflows/
│       ├── ci.yml                         # Test + lint + terraform validate
│       └── deploy.yml                     # Build → scan → push → deploy → rollback
│
├── Jenkinsfile                            # Jenkins pipeline (interview reference)
├── Dockerfile                             # Multi-stage build (builder + runtime)
├── .dockerignore                          # Excludes secrets, infra, tests, git
├── docker-compose.yml                     # Base stack (API, worker, PG, Redis)
├── docker-compose.override.yml            # Local dev: hot reload + Flower UI
├── alembic.ini                            # Alembic configuration
├── pytest.ini                             # Pytest configuration + asyncio mode
├── requirements.txt                       # Python dependencies (pinned versions)
├── .env.example                           # Safe-to-commit env var template
├── README.md                              # DevOps story-first project overview
└── project_structure.txt                  # This file