# =============================================================================
# TaskHub API — Project Structure
# Last updated: Milestone 4 (Kubernetes + Helm)
#
# Milestone history:
#   M1 ✅ Docker production hardening (multi-stage build, .dockerignore, override)
#   M2 ✅ Terraform production structure (remote state, Secrets Manager, modules)
#   M3 ✅ CI/CD hardening (split workflows, Trivy scanning, rollback, Jenkinsfile)
#   M4 ✅ Kubernetes + Helm (manifests, HPA, EKS Terraform, Helm chart)
#   M5 ⏳ Monitoring stack (Prometheus, Grafana, CloudWatch alarms)
#   M6 ⏳ Incident response docs (INCIDENTS.md, OPERATIONS.md)
#   M7 ⏳ Cost & scaling awareness (COST.md, Fargate Spot)
#   M8 ⏳ Interview polish + README overhaul
# =============================================================================

taskhub-api/
│
├── app/                              # FastAPI application (unchanged)
│   ├── main.py
│   ├── config.py
│   ├── db.py
│   ├── security.py
│   ├── events.py
│   ├── idempotency.py
│   ├── routes/
│   ├── schemas/
│   ├── models/
│   ├── workers/
│   └── tests/
│
├── infra/
│   ├── ecs-tasks/
│   │   ├── api-task-definition.json
│   │   └── worker-task-definition.json
│   └── terraform/
│       ├── main.tf
│       ├── iam.tf
│       ├── ecr.tf
│       ├── lambda.tf
│       ├── migration-task.tf
│       ├── outputs.tf
│       ├── provider.tf               # M2: remote state
│       ├── variables.tf              # M2: updated
│       ├── ecs.tf                    # M2: Secrets Manager ARNs
│       ├── secrets.tf                # M2: Secrets Manager resources
│       ├── monitoring.tf             # M2: CloudWatch alarms
│       ├── terraform.tfvars.example  # M2: safe example values
│       └── .terraform.lock.hcl
│       # ── M4 ──────────────────────────────────────────────────────────────
│       └── eks.tf                    # ✅ NEW: EKS cluster (interview reference)
│       # ────────────────────────────────────────────────────────────────────
│
│
# ── M4: Kubernetes Manifests ─────────────────────────────────────────────────
├── k8s/
│   ├── configmap.yaml                # ✅ NEW: non-sensitive env vars
│   ├── deployment-api.yaml           # ✅ NEW: readiness/liveness/startup probes
│   ├── deployment-worker.yaml        # ✅ NEW: Celery worker, exec liveness probe
│   ├── service.yaml                  # ✅ NEW: ClusterIP + ALB Ingress
│   └── hpa.yaml                      # ✅ NEW: CPU + memory autoscaling
│
├── helm/
│   └── taskhub/
│       ├── Chart.yaml                # ✅ NEW: chart metadata
│       ├── values.yaml               # ✅ NEW: all defaults, overridable per env
│       └── templates/
│           ├── _helpers.tpl          # ✅ NEW: reusable label helpers
│           ├── configmap.yaml        # ✅ NEW
│           ├── deployment-api.yaml   # ✅ NEW
│           ├── deployment-worker.yaml # ✅ NEW
│           ├── service.yaml          # ✅ NEW
│           └── hpa.yaml              # ✅ NEW
# ─────────────────────────────────────────────────────────────────────────────
│
├── alembic/
│   └── versions/
│
├── docs/
│   ├── ARCHITECTURE.md
│   ├── SECURITY.md
│   ├── FAILURE_MODES.md
│   └── ECR.md
│
├── scripts/
│   ├── push-to-ecr.sh
│   └── run-migrations.sh
│
├── .github/
│   └── workflows/
│       ├── ci.yml                    # M3: test + validate
│       └── deploy.yml                # M3: build → scan → deploy → rollback
│
├── Jenkinsfile                       # M3: Jenkins pipeline reference
├── Dockerfile                        # M1: multi-stage
├── .dockerignore                     # M1
├── docker-compose.yml
├── docker-compose.override.yml       # M1: local dev
├── pytest.ini
├── requirements.txt
├── .env.example
├── README.md
└── project_structure.txt             # ✅ UPDATED this file